# Steps to generate the final, viewported image after
# you've already exported it from xfig

# Is it possible to use xfig from the command line?
# xfig -exportL pdftex domain2.fig

all:
	pdflatex domain2_input.tex
	pdflatex domain2_viewport.tex
